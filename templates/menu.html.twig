{% extends '@KnpMenu/menu.html.twig' %}

{% block root %}
    {% set listAttributes = item.childrenAttributes|merge({'class': 'nav navbar-nav'}) %}
    {{ block('list') -}}
{% endblock %}

{% block item %}
    {% set options = options|merge({'branch_class': 'dropdown'}) %}

    {{ parent() }}
{% endblock %}